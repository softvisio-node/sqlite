name: build

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - master
  # pull_request:
  #   branches:
  #     - master
  # release:
  #   types:
  #     - released

jobs:
  build:
    # if: ${{ github.event_name == 'release' }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          # - macos-latest
    name: Prebuild on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: publish
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 17
      - run: |
          tar --version
          sed --version
      # - run: npm install --ignore-scripts
      # - run: npx --no-install prebuild --strip --include-regex 'better_sqlite3.node$' -u ${{ secrets.GITHUB_TOKEN }} -r node
      # - run: npx --no-install prebuild --strip --include-regex "better_sqlite3.node$" -u ${{ secrets.GITHUB_TOKEN }} -r electron -t 15.0.0
